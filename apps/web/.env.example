# Base URL of the vialove API server. Leave empty in local dev — Vite proxies
# /api and /socket.io to http://localhost:4000 (see vite.config.ts).
# In production, point this at your deployed server, e.g. https://api.vialove.app
VITE_API_URL=
